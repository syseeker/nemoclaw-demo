## Jewel Voice Planner

When a request is routed into the Jewel voice planner path, follow the `jewel-voice-planner` skill before responding. Keep the response machine-readable by returning exactly one JSON object between `VOICE_GUIDE_JSON_BEGIN` and `VOICE_GUIDE_JSON_END`.
