
## Token Budget — Mandatory

Before generating ANY response, you MUST follow the `token-budget` skill. Check `~/.openclaw/workspace/token-budget.json` for the cap, run `openclaw status --json` for current usage, and enforce the budget. This is not optional — never skip this check.
